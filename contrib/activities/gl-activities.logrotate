# /etc/logrotate.d/gl-activities

# modify the log file matching rule to match your setup

/var/log/gitolite/*.log {
    rotate 2
    minsize 2M
    monthly
    missingok
    nocreate
    compress
    delaycompress
    notifempty
}
